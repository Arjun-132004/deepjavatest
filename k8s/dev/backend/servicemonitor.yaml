apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: deepjavatest-backend
  namespace: storefrontproject123
  labels:
    app.kubernetes.io/name: deepjavatest-backend
    app.kubernetes.io/managed-by: deepagent
    release: monitoring
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: deepjavatest-backend
  endpoints:
    - port: http
      path: /metrics
      interval: 30s

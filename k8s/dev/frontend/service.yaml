apiVersion: v1
kind: Service
metadata:
  name: deepjavatest-frontend
  namespace: storefrontproject123
  labels:
    app.kubernetes.io/name: deepjavatest-frontend
    app.kubernetes.io/managed-by: deepagent
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: deepjavatest-frontend
  ports:
    - name: http
      port: 80
      targetPort: 8080
